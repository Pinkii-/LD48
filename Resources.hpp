#ifndef RESOURCES_HPP
#define RESOURCES_HPP
#include "utils.hpp"

class Resources {
public:
    static void load();
    static sf::Texture texture;
    static sf::Font font;
};

#endif // RESOURCES_HPP
